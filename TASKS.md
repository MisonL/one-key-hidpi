# Intel Hackintosh HiDPI Tasks

此文件是本 fork 中 Intel 黑苹果自动 HiDPI 功能的唯一任务来源。

永久运行约束：不得重启、睡眠或唤醒主机，不得热插拔或重新连接显示器，不得重载显示服务或重初始化显示子系统，也不得再对真实显示器执行 Intel `apply` 或 `revert`。后续验证只能使用只读检查或隔离 fixture。

| ID | 标题 | 内容 | 验收标准 | 审查要求 | 状态 | 标签 |
| --- | --- | --- | --- | --- | --- | --- |
| IH-001 | 只读显示器盘点 | 新增独立命令，读取 Intel 外接显示器 EDID、首选时序、目标 override 路径及已有模式，不写入系统目录。 | fixture 可识别多台显示器、解析原生分辨率和已有模式；无有效 EDID 时显式失败。 | 检查实现没有 sudo、defaults write 或写入 Overrides 的路径。 | 已完成 | 高优先级 |
| IH-002 | 相近 HiDPI 模式生成器 | 根据确认后的生成规则产生有限预设，验证输入、取整、去重和 framebuffer 上限。 | fixture 覆盖正常、重复、非法和超限输入；生成结果稳定。 | 确认不从未验证的系数表推导模式。 | 已完成 | 高优先级 |
| IH-003 | 安全写入和精确回退 | 合并目标 plist，创建精确备份和 manifest，校验后原子替换；回退仅恢复本工具修改的目标。 | fixture 验证无覆盖无关字段、失败不写入、回退不删除同厂商其他显示器。 | 检查权限、所有权、原子替换和恢复范围。 | 已完成 | 高优先级 |
| IH-004 | 交互入口和无写入验收 | 在原脚本中增加 Intel 安全 HiDPI 可选菜单项，自动读取选中 EDID 的原生分辨率并调用独立安全工具。 | 旧选项编号与写入路径不变；预览和取消流程不写入系统目录；apply/revert 要求 root 和二次确认。 | 检查新菜单不调用 legacy 写入或删除函数。 | 已完成 | 高优先级 |
| IH-006 | 边界加固与回归 | 修复单文件入口依赖、分辨率整数溢出、EDID 校验、失败目录清理、备份元数据恢复和路径并发边界。 | 单文件保留旧菜单；无效输入无残留；回退保留原文件元数据；符号链接和锁冲突显式失败。 | 覆盖输入、路径、回退、失败清理和并发锁回归，并记录无法消除的外部 TOCTOU 边界。 | 已完成 | 高优先级 |
| IH-007 | EDID 和盘点输出加固 | 校验完整 EDID 基础块及校验和，净化显示名称，统一盘点 ID，并明确报告损坏 override。 | 错误校验和、控制字符、前导零 ID 和损坏 plist 均有可重复 fixture 回归；有效盘点保持不变。 | 检查盘点命令仍只读、输出不含控制字符、路径与 apply ID 规范一致。 | 已完成 | 高优先级 |
| IH-008 | Override 根绑定与目标锁 | 将 manifest 绑定规范化 override 根，并将协作锁绑定到目标 override 路径而不是 state 根。 | 不同 state 根无法并发修改同一 target；错误 override 根的回退显式失败且不触碰其 target。 | 覆盖并发锁、manifest 根匹配、失败清理和旧状态拒绝。 | 已完成 | 高优先级 |
| IH-009 | 旧菜单安全回退与文档 | 将旧菜单的单显示器回退缩小到产品 override，并更新双语恢复说明以区分旧菜单和 Intel 安全菜单。 | 单显示器回退保留同厂商其他 override；文档不再把宽泛删除路径描述为安全恢复。 | 覆盖旧菜单回退范围、双语文档命令与安全菜单回退入口。 | 已完成 | 高优先级 |
| IH-010 | 旧菜单孤立附件回退 | 当前产品主 override 缺失时，仍精确移除该产品遗留的图标附件。 | 旧菜单和生成的恢复脚本均能移除当前产品的孤立 `.icns` / `.tiff`，保留同厂商其他产品。 | 覆盖直接菜单、生成恢复脚本、悬挂符号链接和 sibling 保留。 | 已完成 | 高优先级 |
| IH-011 | 复核后回退与并发边界加固 | 拒绝旧菜单经 vendor 符号链接删除；清理当前产品后保留 legacy vendor 目录，避免目录名称替换竞态；在 Intel target 缺失时按已验证 manifest 恢复原文件或清理自建状态；新建 target 使用无覆盖提交。 | vendor 符号链接外部目标保持不变、同厂商 sibling 和 legacy vendor 目录保留；删掉 target 后回退能恢复原文件或精确清理状态；新建提交不覆盖竞争方文件。 | 覆盖主菜单和生成恢复脚本、state/manifest 清理、无覆盖安装原语、目录隔离无覆盖标志及测试初始化失败路径。 | 已完成 | 高优先级 |
| IH-012 | 恢复脚本与 swap 失败安全收口 | 恢复脚本以私有临时文件原子发布并拒绝既有链接或硬链接；首次 swap 后任一身份或内容校验失败时绝不执行第二次 swap，保留 target 与 staged 工件并显式失败。 | 隔离 fixture 覆盖恢复脚本符号链接、硬链接和生成失败保留原内容；覆盖 staged 文件被竞争方替换时，target 保留首次 swap 的结果且不得发生自动交换。 | 检查没有触碰系统目录；验证恢复路径不覆盖竞争方文件、首次 swap 外无额外交换、原文件和恢复工件语义明确。 | 已完成 | 高优先级 |
| IH-013 | swap 前内容校验收口 | replace 在首次 swap 前重新校验 target 和 staged 文件内容，并固定竞态断言的操作前候选哈希。 | 隔离 fixture 覆盖 target 同 inode 内容变化时不执行首次 swap；断言使用操作前固定哈希。 | 检查仍只有一次 swap、失败不覆盖 target、无系统目录写入。 | 已完成 | 高优先级 |
| IH-014 | 系统根与状态根信任绑定 | 禁止系统 Overrides 根与自定义 state 根混用，也禁止系统 state 根与自定义 Overrides 根混用，避免未认证 state 被用作特权恢复源。 | fixture 覆盖系统/系统、自定义/自定义及两种混用组合；混用在创建目录或读取 manifest 前显式失败且无写入。 | 检查路径规范化后再判定根类型；确认默认路径和隔离测试路径行为不变。 | 已完成 | 高优先级 |
| IH-015 | Recovery 路径与全量删除边界 | 生成的恢复脚本从脚本自身位置解析并验证系统卷根，不再依赖启动时当前目录；移除或收紧整棵 Overrides 的递归删除，并传播删除失败。 | 从任意当前目录启动仍只定位脚本所属标准用户目录对应的卷；错误布局显式失败；失败时不得打印成功；fixture 不触碰真实系统目录。 | 覆盖错误 CWD、非标准脚本位置、删除失败和同卷 sibling 保留；确认没有宽泛 `rm -rf`。 | 已完成 | 高优先级 |
| IH-016 | 跨启动会话持久身份回退保护 | 将 manifest 升级为 v5，记录 boot session 与 `inode:birthtime` 持久身份；apply/revert 的 target、backup、restore 和清理路径统一核验该身份。v4 状态必须由 `--migrate-v4` 显式确认后，完成哈希和 inode 校验并原子迁移。 | fixture 覆盖 v4 缺少确认时拒绝、迁移失败保留 v5 状态、跨 boot session 的稳定文件精确回退、同内容替换文件拒绝及既有竞争态回归；无默认系统目录写入。 | 追踪所有新增持久身份调用点；确认只有隔离 fixture 写入；检查无隐式 v4 降级、无二次 swap 和无系统重载调用。 | 已完成 | 高优先级 |
| IH-005 | Intel 实机精确回退验收（无系统重载） | 对用户明确指定的 Lenovo `30ae:62a5` 完成精确回退后验。永久禁止重启、睡眠/唤醒、热插拔、显示服务重载，以及再次对真实显示器运行 apply 或 revert。 | 记录目标、已执行的精确回退命令、回退后原始哈希、无 manifest/backup/lock 残留和 4K `10ac:41fe` 未变；明确不宣称任何被禁止的显示生效验证。 | 检查只涉及 `30ae:62a5`；核对 `10ac:41fe` 哈希、Lenovo plist 和状态目录；所有后续回归仅使用隔离 fixture。 | 已完成 | 无系统重载 |
| IH-017 | 目标绑定的运行时模式验收 | 新增只读 `verify-modes` 命令，按厂商/产品 ID 枚举目标显示器并将候选逻辑尺寸与 framebuffer 同时比对；支持已捕获的模式记录用于离线复验。 | 全量命中返回成功；部分命中显式返回非零并列出缺失模式；目标不符或记录格式错误显式失败；不写入默认系统目录。 | 覆盖全量、部分、目标不符和无效记录 fixture；检查无 sudo、无 override 写入、无重启或显示服务调用。 | 已完成 | 高优先级 |
| IH-018 | 平滑缩放模式集与近原生 HiDPI | 为 `preview`、`apply` 和 `verify-modes` 增加显式 `smooth` 模式集：按面板原生宽高比在 2/3 至 1 倍逻辑尺寸间生成密集的 2x HiDPI 档位；可显式加入少 1 逻辑像素高的近原生 HiDPI 兼容档。保留现有五档预设为默认行为。 | `1920x1080` 的 smooth 预览稳定生成 `1280x720` 至 `1920x1080` 的 41 个 16 字节 HiDPI 档位，步进为 `16x9`；启用近原生选项后额外生成 `1920x1079`；隔离 apply 仅合并缺失 payload，完整离线捕获能通过目标绑定验收；旧预设输出不变。 | 检查所有新路径只写隔离 fixture；验证 framebuffer 上限、精确宽高比、去重、参数冲突和近原生边界；确认无 sudo、无系统 override 写入、无重启或显示服务调用。 | 已完成 | 高优先级 |
| IH-019 | 封存旧版直接写入入口 | 将 `hidpi.sh` 收敛为完整本地仓库中的 Intel 安全交互入口，移除普通菜单中可触发的旧版生成、下载、提权复制和 `defaults write` 路径。 | 本地入口仅分派到 Intel 安全菜单；缺失安全依赖时显式失败；生产入口不包含 `sudo`、`curl`、`defaults write`、遗留分辨率生成或递归复制删除命令。 | 用隔离测试覆盖完整入口、缺失依赖和取消分支；检查无真实系统路径写入，ShellCheck 无错误。 | 已完成 | 高优先级 |
| IH-020 | BetterDisplay 相似分辨率兼容集 | 基于对 `172.16.84.121` 两台 1920x1080 Dell 显示器的只读 override 取证，为 smooth 模式增加显式相似分辨率兼容开关：对每个 HiDPI 候选同时生成逻辑尺寸和 2x framebuffer 尺寸的普通 8-byte payload；默认行为不变。 | 对 `1920x1080` 的 `smooth + near-native + similar`，生成 42 个 16-byte HiDPI payload 与 84 个去重的 8-byte 普通 payload，集合逐项等于取证到的 BetterDisplay 集合；preview、隔离 apply 和离线 verify 一致；不写默认系统路径。 | 先运行失败测试；检查参数冲突、去重、默认兼容、manifest 元数据、回退精确性和无 sudo/显示服务调用；真实显示器仅做只读比对。 | 已完成 | 高优先级 |
