# IH-028 验证记录

## 范围

本任务将仓库全部 Markdown 文档的叙述性内容、标题、表头和审查说明统一为中文。命令、路径、
文件名、参数、提交号、payload、API 名称、模型名、代码标识、URL 和历史测试输出保持原样，
以便文档仍可直接执行和审计。

处理范围包括：

- `README.md` 和 `README-zh.md`；
- `INTEL_HACKINTOSH_HIDPI_RESEARCH.md` 和 `TASKS.md`；
- `docs/reviews/` 下的全部审查记录。

## 已完成内容

- 将默认 `README.md` 从英文说明改为中文说明，并保留前提条件、只读盘点、交互入口、命令行、
  精确回退、限制和参考来源章节；
- 将 IH-002 至 IH-019 的历史验证记录翻译为中文，保留每个任务的实现边界、命令、退出码、
  fixture 证据、外部复核结果、复杂度豁免和永久禁止的真实系统操作；
- 将中文 README 的语言链接和任务台账标题统一为中文；
- 复核 CR-1B608FE、CR-EXTERNAL、CR-FINAL、CR-REMOTE 以及 IH-020 至 IH-027，确认其叙述性
  内容已经是中文；未改写历史提交基线或审查结论；
- 保留所有技术标识的原文写法，例如 `HiDPI`、`EDID`、`payload`、`manifest`、`fixture`、
  `apply`、`revert`、`CodeRabbit`、`Claude`、`OMP`、命令、路径和 URL。

## 验证证据

本任务只修改 Markdown 和 `TASKS.md`，未修改业务代码，未使用 `sudo`，未执行真实显示器
`apply`/`revert`，未写入系统 Overrides，未重载显示服务，未重启、睡眠、唤醒或热插拔显示器。

英文残留扫描按 Markdown 代码块分段检查。扫描结果中保留的 ASCII 内容均属于技术标识、命令、
路径、文件名、提交号、URL、测试输出、产品名称，或为既有 fixture 所要求的兼容性原文索引；
没有发现英文标题或英文叙述段落。README 中的兼容性索引位于 HTML 注释，不会显示为用户界面文本。

## 结论

仓库 Markdown 文档已统一为中文记录，IH-028 的验收标准和审查要求满足。该任务不改变任何
代码行为或真实系统状态。
