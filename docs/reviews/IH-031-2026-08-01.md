# IH-031 验证记录

## 范围

本任务收口 Markdown 中文化和 Claude/OMP 外部复核的证据可复现性。记录固定当前提交、上游提交、命令范围、退出码和脱敏摘要哈希；临时会话文件不作为唯一通过依据。

验证基线:

- 当前提交: `160142a28259d6c0f89ccbf560c23234ca31e8e4`;
- 上游提交: `aa4c0b500da2498c2d31b7aec0ffb9993959a526`;
- 分支状态: `master` 与 `origin/master` 一致，工作区无未提交改动。

本任务没有使用 `sudo`，没有执行真实显示器 `apply`/`revert`，没有写入系统 Overrides，没有重载显示服务，没有重启、睡眠、唤醒或热插拔显示器。

## 文档扫描

扫描范围由 Git 已跟踪的 `*.md` 文件固定。IH-028 中的中文叙述扫描、Markdown 围栏和相对本地链接扫描命令在当前基线退出码均为 `0`，未发现疑似英文叙述、未闭合围栏或缺失本地目标。完整命令保留在 [IH-028-2026-07-31.md](IH-028-2026-07-31.md) 的验证章节，避免重复维护两份扫描脚本。

```sh
git ls-files -- '*.md'
```

上述扫描只读，不创建临时文件，也不触碰系统显示器路径。

## 外部复核证据

`docs/reviews/CR-EXTERNAL-2026-07-31.md` 固化了两个有正文且退出码为 `0` 的窄范围复核:

| 复核方 | CLI/模型 | 会话 | 退出码 | 结论 |
| --- | --- | --- | --- | --- |
| Claude | `2.1.220`，请求 `claude-sonnet-4-6` | `49168cbc-c35c-430b-b294-2931b35aca6f` | `0` | 窄范围内未确认当前输入路径可触发的缺陷 |
| OMP | `17.1.8`，`newapi-anthropic/claude-sonnet-4-6` | `73274` | `0` | 未确认高或中风险的当前可触发问题 |

已固化的脱敏原始工件摘要:

| 工件 | 大小 | SHA-256 |
| --- | ---: | --- |
| Claude JSONL | `243600` 字节 | `36fc053cea5038950d17ea6185faa2e9a5378c766cca7f81a552dbe2d3a5af27` |
| OMP JSONL | `312966` 字节 | `6dae6c45447822fd1cde8830c5b6410bd376cdec81f42d4428110295cfd50327` |

原始工件位于 `/private/tmp`，属于临时存储，可能已被系统清理或移入可恢复废纸篓。缺失只表示临时工件不可用，不否定本记录中已经固化的摘要。另行触发但无正文的 Claude/OMP 请求不计为通过或失败证据。

## 本地代码验证

以下命令在当前基线执行，退出码均为 `0`:

```sh
for shell_script in hidpi.sh intel-hidpi.sh lib/*.sh tests/test_hidpi_safe_entrypoint.sh tests/test_intel_hidpi_*.sh; do /bin/bash -n "$shell_script" || exit 1; done
TERM=xterm-256color shellcheck -x -s bash hidpi.sh intel-hidpi.sh lib/*.sh tests/test_hidpi_safe_entrypoint.sh tests/test_intel_hidpi_*.sh
/usr/bin/swiftc -typecheck lib/intel_hidpi_runtime_modes.swift
git status --short --branch
git diff --check aa4c0b500da2498c2d31b7aec0ffb9993959a526..160142a28259d6c0f89ccbf560c23234ca31e8e4
fixture_status=0
for test_script in tests/test_*.sh; do /bin/bash "$test_script" || fixture_status=1; done
((fixture_status == 0)) || exit 1
```

完整串行 fixture 回归输出 `FULL_FIXTURE_SUITE=PASS`，共 11 项；所有写入均发生在测试创建的临时目录。未执行真实显示器或系统 Overrides 操作。

## 未覆盖边界

- macOS CoreDisplay/IOKit 在真实 Intel 黑苹果上接受并暴露全部候选模式的行为；
- 真实显示器写入、显示服务重载、重启、睡眠/唤醒和热插拔；
- APFS 快照或 Time Machine 挂载点下 `stat.nlink` 语义；
- `renameatx_np` 在 SIP 或沙盒环境中的权限行为；
- `MAX_BULK_PLIST_ARRAY_XML_BYTES` 恰好越界时的真实用户体验；
- `data_payload_list_has_value` 的独立实现级证明和非 Intel GPU 的运行时模式行为。

这些边界已明确记录，不能由隔离 fixture 或静态类型检查推导为真实硬件通过。

## 后续收口说明

IH-032 在本记录之后继续收口三项边界：inventory 的离线输入、override 根和目标文件现在拒绝符号链接并通过安全描述符读取；smooth 模式在计算采样步长前增加数量下界断言；批量 plist 合并通过 FD 传递 payload，避免把完整 payload 放进 Ruby 命令行参数。IH-032 的独立验证记录和最终命令结果应以同目录的后续审查记录为准。

## 结论

IH-031 的命令范围、退出码、外部复核稳定摘要和临时工件边界已固化；文档证据不再依赖临时会话路径作为唯一来源。验收标准和审查要求满足。
