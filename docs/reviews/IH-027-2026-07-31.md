# IH-027 审查记录

## 范围

- 任务：发布文档口径同步。
- 业务基线：`4e6bbc7`。
- 同步内容：双语 README、Intel 调研、最终回归总览、Claude/OMP 外部复核、
  远程 BetterDisplay 对照和 IH-020 历史审查记录。
- 验证边界：只修改文档和任务台账；未执行 sudo、真实显示器 apply/revert、
  系统 Overrides 写入、显示服务重载、重启、睡眠/唤醒或热插拔。

## 同步内容核对

1. README 明确说明 `preset`、`smooth`、近原生和相似分辨率开关的行为，
   普通 payload 的 32 位边界、identical 防御以及不自动重载显示系统的限制。
2. Intel 调研从“尚未实现”更新为当前 fork 的实现状态，同时保留原始调研、
   历史风险和真实硬件未验证边界。
3. CR-FINAL、CR-EXTERNAL、CR-REMOTE 和 IH-020 保留原始提交基线，并显式链接
   IH-026 的当前实现与验证证据，避免把历史结果误读为当前状态。
4. `TASKS.md` 的 IH-027 已标记为“已完成”。

## 验证证据

| 命令 | 结果 |
| --- | --- |
| `git diff --check` | 退出码 0 |
| `bash -n hidpi.sh hidpi.command intel-hidpi.sh lib/*.sh tests/test_hidpi_safe_entrypoint.sh tests/test_intel_hidpi_*.sh` | 退出码 0 |
| `shellcheck -x -s bash hidpi.sh hidpi.command intel-hidpi.sh lib/*.sh tests/test_hidpi_safe_entrypoint.sh tests/test_intel_hidpi_*.sh` | 退出码 0 |
| `/usr/bin/swiftc -typecheck lib/intel_hidpi_runtime_modes.swift` | 退出码 0 |
| `for test_script in tests/test_*.sh; do /bin/bash "$test_script"; done` | 11 个隔离 fixture 全部 PASS，退出码 0 |

CodeRabbit 本轮文档复核请求因免费额度限流未执行完成，因此不将其记为 IH-027
的通过证据。IH-026 业务改动此前已获得 CodeRabbit `findings=0`。

## 结论

文档中的命令、payload 语义、任务状态、验证结果和禁止的真实系统边界已与当前
代码及 `TASKS.md` 对齐。IH-027 验收标准和审查要求满足。
