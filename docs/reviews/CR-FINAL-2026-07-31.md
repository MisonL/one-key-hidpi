# 最终回归与代码审查记录

日期: 2026-07-31
当前业务基线: 4e6bbc7
当前范围: IH-001 至 IH-027 完成后的 master 提交树
原始审查基线: 69ede86

## 当前收口结论

`4e6bbc7` 在原始 IH-001 至 IH-025 回归基础上完成 IH-026 payload 防御性边界收口：

- 相似分辨率同迭代 logical/framebuffer payload 相等时显式失败，失败前不输出模式行。
- 普通 payload 宽高限制为正整数和 `4294967295` 范围。
- 默认 smooth 输出和 BetterDisplay 相似分辨率集合保持不变。

最终 11 项隔离 fixture 串行回归、Bash 语法、ShellCheck、`git diff --check`
均返回退出码 0；CodeRabbit 对 IH-026 业务改动复核返回 `findings: 0`。
当前实现和未覆盖边界详见 `docs/reviews/IH-026-2026-07-31.md`。
本次文档口径同步详见 `docs/reviews/IH-027-2026-07-31.md`。

本次 IH-027 仅修改文档和任务台账。再次调用 CodeRabbit 时命中免费额度限流，
因此不把本次文档 diff 记为 CodeRabbit 已复核；本记录中的 CodeRabbit 结果仅指
IH-026 业务改动复核。

## 原始基线结论

`69ede86` 提交树通过最终隔离 fixture 回归、静态检查和 CodeRabbit 提交基线审查。`TASKS.md` 中 IH-001 至 IH-025 均为“已完成”，验证开始前工作区干净。本轮没有修改业务逻辑。

## 原始基线回归证据

以下测试按固定顺序串行执行，均返回退出码 0，并输出各自的 `PASS` 标记:

    bash tests/test_hidpi_safe_entrypoint.sh
    bash tests/test_intel_hidpi_inventory.sh
    bash tests/test_intel_hidpi_preview.sh
    bash tests/test_intel_hidpi_verify_modes.sh
    bash tests/test_intel_hidpi_menu.sh
    bash tests/test_intel_hidpi_smooth_modes.sh
    bash tests/test_intel_hidpi_smooth_apply_verify.sh
    bash tests/test_intel_hidpi_verify_override.sh
    bash tests/test_intel_hidpi_apply_revert.sh
    bash tests/test_intel_hidpi_root_binding.sh
    bash tests/test_intel_hidpi_storage_races.sh

串行 fixture 回归最终输出为 `FULL_FIXTURE_SUITE=PASS`。所有 fixture 写入均位于 `mktemp` 隔离目录。

以下静态检查均返回退出码 0:

    bash -n hidpi.sh hidpi.command intel-hidpi.sh lib/*.sh tests/test_hidpi_safe_entrypoint.sh tests/test_intel_hidpi_*.sh
    shellcheck -x -s bash hidpi.sh hidpi.command intel-hidpi.sh lib/*.sh tests/test_hidpi_safe_entrypoint.sh tests/test_intel_hidpi_*.sh
    swiftc -typecheck lib/intel_hidpi_runtime_modes.swift
    git diff --check

## 原始基线 CodeRabbit

使用已认证的 CodeRabbit CLI 0.7.1 对已提交基线执行:

    coderabbit review --agent -t committed -c AGENTS.md

审查完成，返回 `findings: 0`。审查覆盖 `69ede86` 提交树中的生产脚本、`lib/`、测试 fixture、测试脚本、README、任务台账和已有审查记录；本报告文件在该次审查之后新增。

## 原始基线安全边界

- 本轮没有执行 `sudo`。
- 没有向默认系统 Overrides 写入，也没有执行真实显示器 `apply` 或 `revert`。
- 没有重载显示服务、重初始化显示子系统、重启、睡眠、唤醒或热插拔显示器。
- 远程主机只读取证不能证明 Intel 驱动在真实系统上会曝光全部候选模式；实机写入和显示服务重载仍属于禁止范围。

## 原始基线工作区

验证开始前 `git status --short --branch` 显示 `master` 相对 `origin/master` ahead 25，未留下未提交业务改动；本报告提交后重新检查工作区应保持干净。
