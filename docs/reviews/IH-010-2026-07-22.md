# IH-010 Verification Record

## Scope

Completed the legacy per-product rollback boundary after a follow-up review
found that the earlier single-display fix only removed icon attachments when
the primary override plist was still present.

Both the legacy menu and generated `.hidpi-disable` helper now test for any of
the three precise product artifacts before removing only those artifacts:

- `DisplayProductID-<id>`
- `DisplayProductID-<id>.icns`
- `DisplayProductID-<id>.tiff`

The condition also detects dangling symbolic links so the link itself can be
removed. The deletion command remains a fixed list of the current product's
three paths and does not traverse the vendor directory.

## Automated Verification

The following commands exited with code 0:

```sh
bash tests/test_intel_hidpi_menu.sh
bash -n hidpi.sh tests/test_intel_hidpi_menu.sh
shellcheck -s bash tests/test_intel_hidpi_menu.sh
git diff --check
```

The menu fixture verifies both direct legacy disable and the generated restore
helper for:

- normal selected plist and attachments;
- orphaned selected `.icns` and `.tiff` files when the plist is absent;
- a selected attachment symbolic link, where only the link is removed and its
  external target remains; and
- sibling product files and attachments under the same vendor directory.

No command in this task wrote to `/Library/Displays` or
`/Library/Application Support`.
