# IH-009 Verification Record

## Scope

Hardened the legacy single-display rollback path and corrected recovery
documentation.

- Legacy menu option 3, choice 1 now removes only the selected display's
  `DisplayProductID` override plus that product's known `.icns` and `.tiff`
  attachments.
- The generated `.hidpi-disable` helper uses the same per-product removal
  scope.
- The explicit legacy "reset all settings" choice remains separate and
  unchanged.
- Chinese and English recovery instructions now distinguish legacy single
  display rollback from Intel safe rollback and no longer recommend recursive
  deletion of the whole Overrides directory.

## Automated Verification

The following commands exited with code 0:

```sh
bash tests/test_intel_hidpi_inventory.sh
bash tests/test_intel_hidpi_preview.sh
bash tests/test_intel_hidpi_apply_revert.sh
bash tests/test_intel_hidpi_menu.sh
bash -n hidpi.sh intel-hidpi.sh lib/intel_hidpi_storage.sh \
  lib/intel_hidpi_menu.sh tests/test_intel_hidpi_inventory.sh \
  tests/test_intel_hidpi_preview.sh tests/test_intel_hidpi_apply_revert.sh \
  tests/test_intel_hidpi_menu.sh
shellcheck -s bash tests/test_intel_hidpi_menu.sh
shellcheck -s bash -x -P "$PWD" intel-hidpi.sh \
  lib/intel_hidpi_storage.sh lib/intel_hidpi_menu.sh \
  tests/test_intel_hidpi_inventory.sh tests/test_intel_hidpi_preview.sh \
  tests/test_intel_hidpi_apply_revert.sh
git diff --check
```

The menu fixture creates two product overrides under one vendor directory. It
verifies that legacy single-display disable removes the selected plist and its
known icon attachments while preserving the sibling plist and attachment. It
also verifies the generated restore helper and both READMEs no longer contain
the broad deletion guidance.

The upstream legacy `hidpi.sh` has pre-existing ShellCheck findings outside
this task's changed paths. Its syntax check passes; the changed menu regression
test itself is ShellCheck-clean.

No command in this task wrote to `/Library/Displays` or
`/Library/Application Support`.
